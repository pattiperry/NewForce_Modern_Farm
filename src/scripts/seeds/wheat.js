//this function of createWheat() returns an object with type, height, and output
export const createWheat = () => {
    return {
        type: "wheat", 
        height: 230,
        output: 6
            }
    
}
