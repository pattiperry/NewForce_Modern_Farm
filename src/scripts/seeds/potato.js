//this function of createPotato() returns an object with type, height, and output
export const createPotato = () => {
    return {
        type: "potato", 
        height: 32,
        output: 2
            }
    
}
