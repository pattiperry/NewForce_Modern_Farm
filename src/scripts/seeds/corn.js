//this function of createCorn() returns an object with type, height, and output
export const createCorn = () => {
    return [
        {
        type: "corn", 
        height: 180,
        output: 6
        },
        {
            type: "corn", 
            height: 180,
            output: 6
        }
    ]
    
}
