//this function of createSoybean() returns an object with type, height, and output
export const createSoybean = () => {
    return {
        type: "soybean", 
        height: 20,
        output: 4
            }
    
}
